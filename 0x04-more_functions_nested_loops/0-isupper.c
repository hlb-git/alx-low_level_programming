/**
* _isupper - takes a date and prints how many days are
* @c: month in number format
* Return: void
*/


int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
