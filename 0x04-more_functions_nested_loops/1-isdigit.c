/**
* _isdigit - takes a date and prints how many days are
* @c: month in number format
* Return: void
*/


int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
