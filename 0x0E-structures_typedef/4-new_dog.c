#include "dog.h"
#include <stdio.h>
#define UNUSED(x) (void)(x)

/**
*new_dog - function name
*@name: content of the struct
*@age: another content of the struct
*@owner: owner parameter in the struct new_dog
*Return: returns a dog struct type
*/


dog_t *new_dog(char *name, float age, char *owner)
{
	UNUSED (name);
	UNUSED (age);
	UNUSED (owner);

	return (0);
}
