#include "main.h"
#include "2-strlen.c"

/**
 * rev_string - converts n to 98
 * @s: the pointer argument
 */

void rev_string(char *s)
{
	int j = _strlen(s);
	int n;

	for (n = j - 1; j >= 0; n--)
	{
		if (n < 0)
		{
			break;
		}

	}
}
